import 'package:flutter/material.dart';
import '../../widgets/admin_layout.dart';

class BusinessApprovalPage extends StatelessWidget {
  const BusinessApprovalPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const AdminLayout(
      title: 'Business Approvals',
      child: Center(
        child: Text('Business Approval Page - Coming Soon'),
      ),
    );
  }
}